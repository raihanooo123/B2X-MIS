/**
 * Order pad (doc 05.1) — placeholder. The row table, sticky footer and
 * bulk entry are built on lib/api/orderPad.ts and lib/money.ts next.
 */
export default function OrderPadIndex() {
    return <h1>Order pad</h1>;
}
